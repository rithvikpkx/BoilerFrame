# Root .env.example used by docker-compose and start-dev.sh
# Copy this to .env and fill values, or export variables in your shell

# Required AWS creds/config (placeholders — replace with your own values)
AWS_ACCESS_KEY_ID=YOUR_AWS_ACCESS_KEY_ID
AWS_SECRET_ACCESS_KEY=YOUR_AWS_SECRET_ACCESS_KEY
AWS_REGION=us-east-1
S3_BUCKET=your-s3-bucket-name
S3_PREFIX=boilerframe

# Optional: override defaults
PORT=4000
MONGODB_URI=mongodb://localhost:27017/boilerframe
